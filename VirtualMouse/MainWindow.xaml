<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Properties="clr-namespace:VirtualMouse.Properties" x:Class="VirtualMouse.MainWindow"
        Title="MainWindow" 
        Height="600" 
        Width="650"
        Loaded="WindowLoaded"
        Closing="WindowClosing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="DebugMsg" Grid.Row="0" Grid.ColumnSpan="2" Text="Debug message..." Foreground="Red" Margin="10,0"/>
        <Viewbox Grid.Row="1" Grid.Column="0" Stretch="Uniform">
            <Image x:Name="depthImage" Width="640" Height="480"/>
        </Viewbox>
        <StackPanel Grid.Row="2" Grid.Column="0" Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Near Field Mode "/>
                <Button x:Name="nearFieldButton" Content="Turn On" Click="nearFieldButton_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
